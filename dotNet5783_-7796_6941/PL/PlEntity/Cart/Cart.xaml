<Page x:Class="PL.PlEntity.Cart.Cart"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      xmlns:local="clr-namespace:PL.PlEntity.Cart"
      mc:Ignorable="d" 
      d:DesignHeight="560" d:DesignWidth="1000"
      Title="Cart">

    <Page.Resources>
        <!--local:DateTimeToString x:Key="DateTimeToString"/>
        <local:StatusToHebrew x:Key="statusToHebrew"/-->

        <Style x:Key="GroupHeaderStyle" TargetType="{x:Type GroupItem}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type GroupItem}">
                        <Expander x:Name="expander" IsExpanded="True"
                                  Background="Transparent"
                                  Foreground="#2b331f">
                            <Expander.Header>
                                <TextBlock Text="{Binding Name}"/>
                            </Expander.Header>
                            <ItemsPresenter/>
                        </Expander>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid Name="MainGrid" ShowGridLines="False" Margin="0,0,0,-6" Background="#FFF6F6F6">
        <Grid.RowDefinitions>
            <RowDefinition Height="77*"/>

        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions >

        <Rectangle HorizontalAlignment="Left"
                   Height="47"
                   VerticalAlignment="Top"
                   Width="1000" 
                   Grid.ColumnSpan="2" Stroke="#FF673AB7" Fill="#FF673AB7" RenderTransformOrigin="0.533,0.405"/>

        <Label x:Name="addProductL"
               Grid.Column="1"
               Content=":פרטי לקוח "
               Style="{StaticResource myTitel_1}"
               Margin="361,4,-86,346"  />

        <Label
               Content=":פרטי הספרים בהזמנה"
               Style="{StaticResource myTitel_1}"
               Margin="174,4,40,437"  />

        <StackPanel Orientation="Horizontal"
            Margin="159,98,25,413" Grid.Column="1">
            <TextBox x:Name="NameC" Text="{Binding CustomerName, Mode=TwoWay}"
                      Height="30" Width="201"/>
            <Label Content="  :שם לקוח"
                Width="97"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal"
            Margin="159,152,25,359" Grid.Column="1">
            <TextBox x:Name="MailC" Text="{Binding CustomerEmail, Mode=TwoWay}"
                Height="30" Width="201"/>
            <Label Content="  :כתובת מייל"
                Width="97"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal"
            Margin="159,210,25,301" Grid.Column="1">
            <TextBox x:Name="AdressC" Text="{Binding CustomerAddress, Mode=TwoWay}"
                Height="30" Width="177"/>
            <Label Content=":כתובת לשליחת ההזמנה"
                Width="141"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal"
            Margin="267,266,21,245" Grid.Column="1">
            <TextBlock Text="{Binding TotalPrice, Mode=TwoWay}"
                Height="30" Width="93"/>
            <Label Content="  :מחיר לתשלום"
                Width="97"/>
        </StackPanel>

        <!--עמוד לפרטי הספרים-->

        <DataGrid x:Name="OrderItems_DateGrid"
                  CanUserAddRows="False"
                  materialDesign:DataGridAssist.CellPadding="10"
                  materialDesign:DataGridAssist.ColumnHeaderPadding="10" 
                  IsTextSearchEnabled="True" 
                  GridLinesVisibility="Horizontal" 
                  AreRowDetailsFrozen="False"
                  AutoGenerateColumns="False"
                  HorizontalGridLinesBrush="Transparent"
                  ScrollViewer.VerticalScrollBarVisibility="Visible" 
                  CanUserDeleteRows="False" 
                  CanUserResizeRows="False"
                  Foreground="Black"
                  IsReadOnly="True"
                  ItemsSource="{Binding}" 
                  HorizontalAlignment="Left"
                  Margin="31,64,0,113"
                  Width="570"
                  Background="{x:Null}" Grid.ColumnSpan="2">

            <DataGrid.GroupStyle>
                <GroupStyle ContainerStyle="{StaticResource ResourceKey=GroupHeaderStyle}">
                    <GroupStyle.Panel>
                        <ItemsPanelTemplate>
                            <DataGridRowsPresenter/>
                        </ItemsPanelTemplate>
                    </GroupStyle.Panel>
                </GroupStyle>
            </DataGrid.GroupStyle>


            <DataGrid.Columns >

                <!--רק חסר מספר הזמנה לאורדראייטם אבל לא קיים עדיין נראה לי-->

                <DataGridTemplateColumn Header="{materialDesign:PackIcon Kind=Trash}"
                                    MinWidth="10">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Style="{StaticResource MaterialDesignFloatingActionMiniButton}"
                                    Height="30" Width="30"
                                    HorizontalAlignment="Center"
                                    Click="deleteOrderItem_Click">
                                <materialDesign:PackIcon Kind="Trash"/>
                            </Button>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn Header="מחיר סופי"    
                                    Binding="{Binding TotalPrice}"
                                    MinWidth="5"/>
                <DataGridTextColumn Header="כמות פריטים"    
                                    Binding="{Binding AmountOfItems}"
                                    MinWidth="5"/>

                <DataGridTemplateColumn Header="{materialDesign:PackIcon Kind=ArrowBottomDropCircleOutline}"
                                    MinWidth="10">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Style="{StaticResource MaterialDesignFloatingActionMiniButton}"
                                    Height="30" Width="30"
                                    HorizontalAlignment="Center"
                                    Click="reduceNum_Click">
                                <materialDesign:PackIcon Kind="ArrowBottomDropCircleOutline"/>
                            </Button>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTemplateColumn Header="{materialDesign:PackIcon Kind=ArrowUpDropCircleOutline}"
                                    MinWidth="10">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Style="{StaticResource MaterialDesignFloatingActionMiniButton}"
                                    Height="30" Width="30"
                                    HorizontalAlignment="Center"
                                    Click="incresNum_Click">
                                <materialDesign:PackIcon Kind="ArrowBottom"/>
                            </Button>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>


                <DataGridTextColumn Header="עלות ליחידה"    
                                    Binding="{Binding PriceOfOneItem}"
                                    MinWidth="5"/>
                <DataGridTextColumn Header="שם הספר"
                                    Binding="{Binding NameOfBook}"
                                    MinWidth="5"/>
                <DataGridTextColumn Header="מספר מוצר "
                                    Binding="{Binding ProductID}"
                                    MinWidth="5"/>
            </DataGrid.Columns>
        </DataGrid>
        <Rectangle Grid.Column="1" HorizontalAlignment="Left" Height="393" Margin="127,62,0,0" Stroke="#FF673AB7" VerticalAlignment="Top" Width="7"/>
        <Button x:Name="MakeOrder" Content="בצע הזמנה" HorizontalAlignment="Left" Margin="104,479,0,0" VerticalAlignment="Top" Click="MakeOrder_Click"/>
        <Button x:Name="CancleOrder" Content="בטל הזמנה" HorizontalAlignment="Left" Margin="263,479,0,0" VerticalAlignment="Top"/>
        <Label Grid.Column="1"  Content="כל השדות הינם חובה*" HorizontalAlignment="Left" Margin="343,59,0,0" VerticalAlignment="Top" Width="124" Foreground="Red" Height="24"/>
    </Grid>
</Page>
